{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node", "config": { "includeFiles": ["build/**", "public/**"] } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server.js" }
  ]
}
